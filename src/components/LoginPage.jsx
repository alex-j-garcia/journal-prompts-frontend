import Layout from "./Layout";
import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <Layout>
      <h2>Login</h2>
      <LoginForm />
    </Layout>
  );
};

export default LoginPage;
