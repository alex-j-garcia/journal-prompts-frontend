import Layout from '@modules/common/components/Layout';

const GlobalFeed = ({ user }) => {
  return (
    <Layout user={user}>
      <h2>Global Feed</h2>
    </Layout>
  );
};

export default GlobalFeed;
